subprojects {
  apply plugin: 'java'
  apply plugin: 'eclipse'

  repositories {
    mavenCentral()
    mavenLocal()
  }

  dependencies {
    testCompile 'junit:junit:4.8.2'
    compile 'junit:junit:4.8.2'
  }

  version = '1.0'

  jar {
    manifest.attributes provider: 'Lonely Robot'
  }
}